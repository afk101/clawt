# Clawt 国际化（i18n）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers-subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Clawt CLI 添加英文/中文双语支持，默认语言改为英文，通过 clawt config 切换。

**Architecture:** 采用函数式 i18n 方案，在消息常量文件内定义双语映射对象，通过 `createMessages()` 工具函数根据当前语言配置自动选择返回值。调用方代码无需改动。散落硬编码中文先迁移到消息常量再统一 i18n 化。

**Tech Stack:** TypeScript, 现有 Commander.js + enquirer + chalk 体系，无新增外部依赖

---

## 文件结构规划

| 操作 | 文件路径 | 职责 |
|------|---------|------|
| Create | `src/utils/i18n.ts` | i18n 核心工具函数（getCurrentLanguage, createMessages） |
| Create | `src/constants/messages/cli-descriptions.ts` | Commander description/option 文本集中管理 |
| Modify | `src/types/config.ts` | ClawtConfig 新增 language 字段 |
| Modify | `src/constants/config.ts` | CONFIG_DEFINITIONS 新增 language 配置项 |
| Modify | `src/constants/messages/common.ts` | 双语化 COMMON_MESSAGES |
| Modify | `src/constants/messages/run.ts` | 双语化 RUN_MESSAGES + 迁移散落中文 |
| Modify | `src/constants/messages/config.ts` | 双语化 CONFIG_CMD_MESSAGES + 迁移散落中文 |
| Modify | `src/constants/messages/merge.ts` | 双语化 MERGE_MESSAGES + 迁移散落中文 |
| Modify | `src/constants/messages/validate.ts` | 双语化 VALIDATE_MESSAGES + 迁移散落中文 |
| Modify | `src/constants/messages/resume.ts` | 双语化 RESUME_MESSAGES + 迁移散落中文 |
| Modify | `src/constants/messages/create.ts` | 双语化 CREATE_MESSAGES |
| Modify | `src/constants/messages/remove.ts` | 双语化 REMOVE_MESSAGES |
| Modify | `src/constants/messages/reset.ts` | 双语化 RESET_MESSAGES |
| Modify | `src/constants/messages/sync.ts` | 双语化 SYNC_MESSAGES |
| Modify | `src/constants/messages/status.ts` | 双语化 STATUS_MESSAGES |
| Modify | `src/constants/messages/alias.ts` | 双语化 ALIAS_MESSAGES |
| Modify | `src/constants/messages/init.ts` | 双语化 INIT_MESSAGES |
| Modify | `src/constants/messages/projects.ts` | 双语化 PROJECTS_MESSAGES |
| Modify | `src/constants/messages/completion.ts` | 双语化 COMPLETION_MESSAGES |
| Modify | `src/constants/messages/home.ts` | 双语化 HOME_MESSAGES |
| Modify | `src/constants/messages/tasks.ts` | 双语化 TASKS_CMD_MESSAGES |
| Modify | `src/constants/messages/cover-validate.ts` | 双语化 COVER_VALIDATE_MESSAGES |
| Modify | `src/constants/messages/post-create.ts` | 双语化 POST_CREATE_MESSAGES |
| Modify | `src/constants/messages/interactive-panel.ts` | 双语化交互面板消息 |
| Modify | `src/constants/messages/update.ts` | 双语化 UPDATE_MESSAGES |
| Modify | `src/constants/messages/index.ts` | 导入并导出 CLI 描述消息 |
| Modify | `src/constants/progress.ts` | TASK_STATUS_LABELS / TEXT_ACTIVITY_PREFIX 双语化 |
| Modify | `src/constants/prompt.ts` | UNKNOWN_DATE_GROUP / GROUP_SEPARATOR_LABEL 双语化 |
| Modify | `src/constants/ai-prompts.ts` | 改为英文 prompt |
| Modify | `src/constants/tasks-template.ts` | TASK_TEMPLATE_CONTENT 双语化 |
| Modify | `src/utils/config-strategy.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/formatter.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/dry-run.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/prompt.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/ui-prompts.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/validate-branch.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/terminal.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/claude.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/task-executor.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/worktree-matcher.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/alias.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/interactive-panel-render.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/validate-runner.ts` | 移除硬编码中文，引用消息常量 |
| Modify | `src/utils/index.ts` | 导出 i18n 工具函数 |
| Modify | `src/index.ts` | 引用 CLI 描述消息，替换硬编码中文 |
| Modify | `src/commands/config.ts` | 引用 CLI 描述消息 + 替换硬编码中文 |
| Modify | `src/commands/init.ts` | 引用 CLI 描述消息 |
| Modify | `src/commands/create.ts` | 引用 CLI 描述消息 + 替换硬编码中文 |
| Modify | `src/commands/remove.ts` | 引用 CLI 描述消息 + 替换硬编码中文 |
| Modify | `src/commands/validate.ts` | 引用 CLI 描述消息 + 替换硬编码中文 |
| Modify | `src/commands/resume.ts` | 引用 CLI 描述消息 + 替换硬编码中文 |
| Modify | `src/commands/run.ts` | 引用 CLI 描述消息 |
| Modify | `src/commands/merge.ts` | 引用 CLI 描述消息 + 替换硬编码中文 |
| Modify | `src/commands/sync.ts` | 引用 CLI 描述消息 + 替换硬编码中文 |
| Modify | `src/commands/status.ts` | 引用 CLI 描述消息 + 替换硬编码中文 |
| Modify | `src/commands/projects.ts` | 引用 CLI 描述消息 |
| Modify | `src/commands/home.ts` | 引用 CLI 描述消息 |
| Modify | `src/commands/reset.ts` | 引用 CLI 描述消息 + 替换硬编码中文 |
| Modify | `src/commands/list.ts` | 引用 CLI 描述消息 + 替换硬编码中文 |
| Modify | `src/commands/alias.ts` | 引用 CLI 描述消息 |
| Modify | `src/commands/completion.ts` | 引用 CLI 描述消息 |
| Modify | `src/commands/tasks.ts` | 引用 CLI 描述消息 |
| Modify | `src/commands/cover-validate.ts` | 引用 CLI 描述消息 + 替换硬编码中文 |
| Modify | `src/hooks/post-create.ts` | 替换硬编码中文 |
| Modify | `README.md` | 顶部添加 GitHub 仓库地址 |
| Modify | `README.zh-CN.md` | 顶部添加 GitHub 仓库地址 |
| Create | `tests/unit/utils/i18n.test.ts` | i18n 工具函数测试 |

---

### Task 1: 类型与配置扩展

**Files:**
- Modify: `src/types/config.ts`
- Modify: `src/constants/config.ts`

- [ ] **Step 1: 扩展 ClawtConfig 类型**

在 `src/types/config.ts` 的 `ClawtConfig` 接口中新增 `language` 字段：

```typescript
/** 界面语言：en（英文）、zh-CN（中文） */
language: 'en' | 'zh-CN';
```

- [ ] **Step 2: 扩展 CONFIG_DEFINITIONS**

在 `src/constants/config.ts` 的 `CONFIG_DEFINITIONS` 中新增 `language` 配置项（放在 `autoDeleteBranch` 之前，作为首个配置项）：

```typescript
language: {
  defaultValue: 'en',
  description: '界面语言：en（英文）、zh-CN（中文）',
  allowedValues: ['en', 'zh-CN'] as const,
},
```

- [ ] **Step 3: 运行测试验证无破坏**

Run: `npx vitest run`
Expected: 所有测试 PASS

- [ ] **Step 4: Commit**

```bash
git add src/types/config.ts src/constants/config.ts
git commit -m "feat(i18n): add language config field to ClawtConfig"
```

---

### Task 2: i18n 核心工具函数

**Files:**
- Create: `src/utils/i18n.ts`
- Modify: `src/utils/index.ts`
- Create: `tests/unit/utils/i18n.test.ts`

- [ ] **Step 1: 编写 i18n 工具函数失败测试**

```typescript
// tests/unit/utils/i18n.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest';

describe('i18n', () => {
  describe('getCurrentLanguage', () => {
    it('should return en when language config is en', async () => {
      const { getCurrentLanguage } = await import('../../../src/utils/i18n.js');
      // Mock loadConfig to return en
      vi.doMock('../../../src/utils/config.js', () => ({
        loadConfig: () => ({ language: 'en' }),
      }));
      expect(getCurrentLanguage()).toBe('en');
    });

    it('should return zh-CN when language config is zh-CN', async () => {
      const { getCurrentLanguage } = await import('../../../src/utils/i18n.js');
      vi.doMock('../../../src/utils/config.js', () => ({
        loadConfig: () => ({ language: 'zh-CN' }),
      }));
      expect(getCurrentLanguage()).toBe('zh-CN');
    });

    it('should default to en when language is not set', async () => {
      const { getCurrentLanguage } = await import('../../../src/utils/i18n.js');
      vi.doMock('../../../src/utils/config.js', () => ({
        loadConfig: () => ({}),
      }));
      expect(getCurrentLanguage()).toBe('en');
    });
  });

  describe('createMessages', () => {
    it('should return en messages when language is en', async () => {
      const { createMessages, setCurrentLanguage } = await import('../../../src/utils/i18n.js');
      setCurrentLanguage('en');
      const messages = createMessages({
        FOO: { en: 'Hello', 'zh-CN': '你好' },
        BAR: { en: (n: number) => `${n} items`, 'zh-CN': (n: number) => `${n} 个` },
      });
      expect(messages.FOO).toBe('Hello');
      expect(messages.BAR(3)).toBe('3 items');
    });

    it('should return zh-CN messages when language is zh-CN', async () => {
      const { createMessages, setCurrentLanguage } = await import('../../../src/utils/i18n.js');
      setCurrentLanguage('zh-CN');
      const messages = createMessages({
        FOO: { en: 'Hello', 'zh-CN': '你好' },
        BAR: { en: (n: number) => `${n} items`, 'zh-CN': (n: number) => `${n} 个` },
      });
      expect(messages.FOO).toBe('你好');
      expect(messages.BAR(3)).toBe('3 个');
    });
  });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `npx vitest run tests/unit/utils/i18n.test.ts`
Expected: FAIL (module not found)

- [ ] **Step 3: 实现 i18n 工具函数**

```typescript
// src/utils/i18n.ts
import { loadConfig } from './config.js';

/** 支持的语言类型 */
export type Language = 'en' | 'zh-CN';

/** 当前语言缓存 */
let currentLanguage: Language | null = null;

/**
 * 获取当前语言配置
 * 优先使用缓存，缓存不存在时从配置文件读取
 * @returns {Language} 当前语言
 */
export function getCurrentLanguage(): Language {
  if (currentLanguage !== null) {
    return currentLanguage;
  }
  try {
    const config = loadConfig();
    currentLanguage = (config.language as Language) || 'en';
  } catch {
    currentLanguage = 'en';
  }
  return currentLanguage;
}

/**
 * 设置当前语言（用于测试和 CLI 初始化时）
 * @param {Language} lang - 语言代码
 */
export function setCurrentLanguage(lang: Language): void {
  currentLanguage = lang;
}

/**
 * 重置语言缓存（配置变更后调用，使下次读取时重新加载）
 */
export function resetLanguageCache(): void {
  currentLanguage = null;
}

/**
 * 创建国际化消息对象
 * 根据当前语言从双语映射中选择对应的文本或函数
 * @param {Record<string, { en: T; 'zh-CN': T }>} i18nMap - 双语消息映射
 * @returns {Record<string, T>} 当前语言的消息对象
 */
export function createMessages<T>(i18nMap: Record<string, { en: T; 'zh-CN': T }>): Record<string, T> {
  const lang = getCurrentLanguage();
  const result: Record<string, T> = {};
  for (const key of Object.keys(i18nMap)) {
    result[key] = i18nMap[key][lang];
  }
  return result;
}
```

- [ ] **Step 4: 在 src/utils/index.ts 中导出 i18n 工具函数**

添加导出：
```typescript
export { getCurrentLanguage, setCurrentLanguage, resetLanguageCache, createMessages } from './i18n.js';
```

- [ ] **Step 5: 运行测试验证通过**

Run: `npx vitest run tests/unit/utils/i18n.test.ts`
Expected: PASS

- [ ] **Step 6: 运行全量测试验证无破坏**

Run: `npx vitest run`
Expected: 所有测试 PASS

- [ ] **Step 7: Commit**

```bash
git add src/utils/i18n.ts src/utils/index.ts tests/unit/utils/i18n.test.ts
git commit -m "feat(i18n): add i18n utility functions (getCurrentLanguage, createMessages)"
```

---

### Task 3: 消息常量双语化 — messages/common.ts

**Files:**
- Modify: `src/constants/messages/common.ts`

- [ ] **Step 1: 将 COMMON_MESSAGES 改为双语映射 + createMessages 包装**

所有 16 个消息常量从直接字符串/函数改为 `{ en, 'zh-CN' }` 双语映射，再通过 `createMessages()` 包装导出。保持 `as const` 断言。

示例关键消息英文翻译：
- `NOT_MAIN_WORKTREE`: `'Please run clawt in the root directory of the main worktree'`
- `NOT_GIT_REPO`: `'Current directory is not a git repository. Please run git init and make an initial commit, then run clawt init'`
- `GIT_NOT_INSTALLED`: `'Git is not installed or not in PATH. Please install Git first'`
- `BRANCH_EXISTS`: `(name: string) => \`Branch ${name} already exists, cannot create\``
- `BRANCH_NAME_EMPTY`: `(original: string) => \`Branch name "${original}" contains no valid characters, cannot create branch\``
- `BRANCH_SANITIZED`: `(original: string, sanitized: string) => \`Branch name sanitized: ${original} → ${sanitized}\``
- `WORKTREE_CREATED`: `(count: number) => \`✓ Created ${count} worktree(s)\``
- `WORKTREE_REMOVED`: `(path: string) => \`✓ Removed worktree: ${path}\``
- `NO_WORKTREES`: `'(No worktrees)'`
- `WORKTREE_NOT_FOUND`: `(name: string) => \`Worktree ${name} does not exist\``
- `MAIN_WORKTREE_DIRTY`: `'Main worktree has uncommitted changes. Please resolve first'`
- `TARGET_WORKTREE_CLEAN`: `'No changes on this worktree branch, nothing to validate'`
- `DESTRUCTIVE_OP_CANCELLED`: `'Operation cancelled'`
- `COMMIT_MESSAGE_REQUIRED`: `'Please provide a commit message (-m option)'`
- `CONFIG_CORRUPTED`: `'Config file corrupted or unparseable, regenerated default config'`
- `WORKTREE_STATUS_UNAVAILABLE`: `'(Status unavailable)'`
- `SEPARATOR`: `'────────────────────────────────────────'`
- `DOUBLE_SEPARATOR`: `'════════════════════════════════════════'`
- `GUARD_BRANCH_NOT_EXISTS`: `(branchName: string) => \`Configured main work branch ${branchName} no longer exists. Please run clawt init to reset\``
- `GUARD_BRANCH_MISMATCH`: `(configuredBranch: string, currentBranch: string) => \`Current branch ${currentBranch} does not match configured main work branch ${configuredBranch}. Run clawt init to update\``
- `GIT_INDEX_LOCKED`: `(lockFilePath: string) => \`Git index is locked, cannot proceed\\n  Cause: Lock file exists (possibly from an interrupted git operation)\\n  Lock file path: ${lockFilePath}\\n  Fix: Confirm no other git operations are running, then run:\\n  rm ${lockFilePath}\``
- `GIT_INDEX_LOCK_RETRYING`: `'Git index is locked, retrying...'`

同时添加从 formatter.ts 迁移的新消息：
```typescript
CONFIRM_CONTINUE: { en: 'Continue?', 'zh-CN': '是否继续？' },
NO_CHANGES: { en: 'No changes', 'zh-CN': '无变更' },
UNCOMMITTED_CHANGES: { en: '(uncommitted changes)', 'zh-CN': '(未提交修改)' },
COMMIT_COUNT: { en: (n: number) => `${n} commit(s)`, 'zh-CN': (n: number) => `${n} 个提交` },
JUST_NOW: { en: 'just now', 'zh-CN': '刚刚' },
MINUTES_AGO: { en: (n: number) => `${n} min ago`, 'zh-CN': (n: number) => `${n} 分钟前` },
HOURS_AGO: { en: (n: number) => `${n} hr ago`, 'zh-CN': (n: number) => `${n} 小时前` },
DAYS_AGO: { en: (n: number) => `${n} day(s) ago`, 'zh-CN': (n: number) => `${n} 天前` },
MONTHS_AGO: { en: (n: number) => `${n} month(s) ago`, 'zh-CN': (n: number) => `${n} 个月前` },
YEARS_AGO: { en: (n: number) => `${n} year(s) ago`, 'zh-CN': (n: number) => `${n} 年前` },
UNKNOWN_ERROR: { en: 'Unknown error', 'zh-CN': '未知错误' },
```

从 ui-prompts.ts 迁移：
```typescript
NON_INTERACTIVE_BRANCH_SELECT: { en: 'Cannot select branch in non-interactive mode. Please use -b to specify branch', 'zh-CN': '非交互模式下无法进行分支选择，请使用 -b 指定分支' },
NON_INTERACTIVE_MULTI_SELECT: { en: 'Cannot multi-select branches in non-interactive mode', 'zh-CN': '非交互模式下无法进行分支多选' },
```

从 worktree-matcher.ts 迁移的时间词：
```typescript
TODAY: { en: 'Today', 'zh-CN': '今天' },
YESTERDAY: { en: 'Yesterday', 'zh-CN': '昨天' },
```

- [ ] **Step 2: 运行测试验证**

Run: `npx vitest run`
Expected: PASS（消息常量值变更，但引用方式不变）

- [ ] **Step 3: Commit**

```bash
git add src/constants/messages/common.ts
git commit -m "feat(i18n): bilingualize COMMON_MESSAGES with en/zh-CN mapping"
```

---

### Task 4: 消息常量双语化 — 其余 20 个 messages 文件

**Files:**
- Modify: `src/constants/messages/run.ts`
- Modify: `src/constants/messages/config.ts`
- Modify: `src/constants/messages/merge.ts`
- Modify: `src/constants/messages/validate.ts`
- Modify: `src/constants/messages/resume.ts`
- Modify: `src/constants/messages/create.ts`
- Modify: `src/constants/messages/remove.ts`
- Modify: `src/constants/messages/reset.ts`
- Modify: `src/constants/messages/sync.ts`
- Modify: `src/constants/messages/status.ts`
- Modify: `src/constants/messages/alias.ts`
- Modify: `src/constants/messages/init.ts`
- Modify: `src/constants/messages/projects.ts`
- Modify: `src/constants/messages/completion.ts`
- Modify: `src/constants/messages/home.ts`
- Modify: `src/constants/messages/tasks.ts`
- Modify: `src/constants/messages/cover-validate.ts`
- Modify: `src/constants/messages/post-create.ts`
- Modify: `src/constants/messages/interactive-panel.ts`
- Modify: `src/constants/messages/update.ts`

- [ ] **Step 1: 逐文件双语化**

对每个消息文件执行与 Task 3 相同的改造模式：
1. 将直接导出的消息常量改为 i18n 映射对象（变量名加 `_I18N` 后缀）
2. 用 `createMessages()` 包装导出
3. 所有中文消息保留为 `'zh-CN'` 版本
4. 新增英文 `en` 版本翻译
5. 迁移对应散落位置的硬编码中文

同时迁移散落中文到对应文件：

**run.ts** 迁移自：
- `dry-run.ts`: `路径:` → `PATH_LABEL`, `任务:` → `TASK_LABEL`
- `task-executor.ts`: `任务执行失败` → `TASK_FAILED`, `全部任务已完成` → `ALL_TASKS_COMPLETED`, `成功:` → `SUCCESS_LABEL`, `失败:` → `FAILURE_LABEL`, `总耗时:` → `TOTAL_DURATION_LABEL`, `总花费:` → `TOTAL_COST_LABEL`

**config.ts** 迁移自：
- `config-strategy.ts`: `(未设置)` → `NOT_SET`, `非交互模式下无法使用交互式配置编辑器...` → `NON_INTERACTIVE_CONFIG_EDITOR`, `请输入有效的数字` → `INVALID_NUMBER_PROMPT`
- `commands/config.ts`: `当前配置将被覆盖为默认值` → `CONFIG_RESET_WARNING`

**validate.ts** 迁移自：
- `validate-branch.ts`: `工作区仍然不干净...` → `WORKSPACE_STILL_DIRTY`, `当前分支有未提交的更改...` → `UNCOMMITTED_CHANGES_ON_BRANCH`, `选择处理方式` → `SELECT_ACTION`, `是否继续执行？` → `CONFIRM_CONTINUE`, `用户选择退出...` → `USER_CHOSE_EXIT`
- `validate-runner.ts`: `指令执行出错，退出码:` → `COMMAND_EXEC_ERROR`, `退出码:` → `EXIT_CODE_LABEL`

**resume.ts** 迁移自：
- `terminal.ts`: `当前不在 cmux 环境中...` → `NOT_IN_CMUX`, `提示：Terminal.app 需要辅助功能权限...` → `TERMINAL_ACCESSIBILITY_HINT`, `批量 resume 目前仅支持 macOS 平台` → `BATCH_RESUME_MACOS_ONLY`
- `claude.ts`: `正在 worktree 中启动 Claude Code 交互式界面...` → `STARTING_CLAUDE_INTERACTIVE`, `分支:` → `BRANCH_LABEL`, `路径:` → `PATH_LABEL`, `指令:` → `COMMAND_LABEL`, `模式:` → `MODE_LABEL`, `继续上次对话` → `CONTINUE_SESSION`, `新对话` → `NEW_SESSION`, `启动 Claude Code 失败:` → `CLAUDE_START_FAILED`

**merge.ts** 迁移自：
- `prompt.ts`: `提交信息不能为空` → `COMMIT_MESSAGE_NOT_EMPTY`
- `commands/merge.ts`: `已配置自动删除...` → `AUTO_DELETE_CONFIGURED`, `是否删除对应的 worktree 和分支` → `CONFIRM_DELETE_WORKTREE_BRANCH`

**alias.ts** 迁移自：
- `utils/alias.ts`: `别名...的目标命令...不存在，已跳过` → `ALIAS_TARGET_NOT_FOUND`

- [ ] **Step 2: 运行测试验证**

Run: `npx vitest run`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add src/constants/messages/
git commit -m "feat(i18n): bilingualize all messages files with en/zh-CN mapping"
```

---

### Task 5: CLI 描述消息集中管理

**Files:**
- Create: `src/constants/messages/cli-descriptions.ts`
- Modify: `src/constants/messages/index.ts`

- [ ] **Step 1: 创建 CLI 描述消息文件**

将所有 Commander `.description()` 和 `.option()` 中的中文文本集中管理为双语映射：

```typescript
// src/constants/messages/cli-descriptions.ts
import { createMessages } from '../../utils/i18n.js';

const CLI_DESCRIPTIONS_I18N = {
  // 主程序
  PROGRAM_DESC: {
    en: 'Run multiple Claude Code Agent tasks in parallel based on Git Worktree',
    'zh-CN': '本地并行执行多个Claude Code Agent任务，融合 Git Worktree 与 Claude Code CLI 的命令行工具',
  },
  OPTION_DEBUG: {
    en: 'Output debug information to terminal',
    'zh-CN': '输出详细调试信息到终端',
  },
  OPTION_YES: {
    en: 'Skip all interactive confirmations, suitable for scripts/CI',
    'zh-CN': '跳过所有交互式确认，适用于脚本/CI 环境',
  },
  // config 命令
  CONFIG_DESC: { en: 'Interactively view and modify global configuration', 'zh-CN': '交互式查看和修改全局配置' },
  CONFIG_RESET_DESC: { en: 'Reset configuration to defaults', 'zh-CN': '将配置恢复为默认值' },
  CONFIG_SET_DESC: { en: 'Set a configuration item (interactive without args)', 'zh-CN': '修改配置项（无参数进入交互式配置）' },
  CONFIG_GET_DESC: { en: 'Get a configuration item value', 'zh-CN': '获取单个配置项的值' },
  // init 命令
  INIT_DESC: { en: 'Initialize project-level configuration, set main work branch', 'zh-CN': '初始化项目级配置，设置主工作分支' },
  INIT_OPTION_BRANCH: { en: 'Specify main work branch name (defaults to current branch)', 'zh-CN': '指定主工作分支名（默认使用当前分支）' },
  INIT_SHOW_DESC: { en: 'Interactively view and modify project configuration (supports --json)', 'zh-CN': '交互式查看和修改项目配置（支持 --json 格式输出）' },
  INIT_OPTION_JSON: { en: 'Output in JSON format', 'zh-CN': '以 JSON 格式输出' },
  // create 命令
  CREATE_DESC: { en: 'Batch create worktrees and branches (with validate branches)', 'zh-CN': '批量创建 worktree 及对应分支（含验证分支）' },
  CREATE_OPTION_NUMBER: { en: 'Number of worktrees to create', 'zh-CN': '创建数量' },
  CREATE_OPTION_POST_CREATE: { en: 'Run postCreate hook (default on, --no-post-create to skip)', 'zh-CN': '执行 postCreate hook（默认开启，--no-post-create 跳过）' },
  // run 命令
  RUN_DESC: { en: 'Batch create worktrees + run Claude Code tasks (supports task files)', 'zh-CN': '批量创建 worktree + 启动 Claude Code 执行任务（支持任务文件）' },
  RUN_OPTION_BRANCH: { en: 'Branch name', 'zh-CN': '分支名' },
  RUN_OPTION_TASKS: { en: 'Task list (can be specified multiple times)', 'zh-CN': '任务列表（可多次指定），不传则在 worktree 中打开 Claude Code 交互式界面' },
  RUN_OPTION_CONCURRENCY: { en: 'Max concurrency, 0 means unlimited', 'zh-CN': '最大并发数，0 表示不限制' },
  RUN_OPTION_FILE: { en: 'Read task list from file (mutually exclusive with --tasks)', 'zh-CN': '从任务文件读取任务列表（与 --tasks 互斥）' },
  RUN_OPTION_DRY_RUN: { en: 'Preview mode, show task plan without executing', 'zh-CN': '预览模式，仅展示任务计划不实际执行' },
  RUN_OPTION_POST_CREATE: { en: 'Run postCreate hook (default on, --no-post-create to skip)', 'zh-CN': '执行 postCreate hook（默认开启，--no-post-create 跳过）' },
  // validate 命令
  VALIDATE_DESC: { en: 'Validate a worktree branch changes in the main worktree', 'zh-CN': '在主 worktree 验证某个 worktree 分支的变更（通过验证分支）' },
  VALIDATE_OPTION_BRANCH: { en: 'Branch name to validate (supports fuzzy match)', 'zh-CN': '要验证的分支名（支持模糊匹配，不传则列出所有分支）' },
  VALIDATE_OPTION_CLEAN: { en: 'Clean up validate state (reset main worktree and delete snapshots)', 'zh-CN': '清理 validate 状态（重置主 worktree 并删除快照）' },
  VALIDATE_OPTION_RUN: { en: 'Command to run in main worktree after successful validation', 'zh-CN': 'validate 成功后在主 worktree 中执行的命令' },
  // merge 命令
  MERGE_DESC: { en: 'Merge a validated worktree branch into the main worktree', 'zh-CN': '合并某个已验证的 worktree 分支到主 worktree' },
  MERGE_OPTION_BRANCH: { en: 'Branch to merge (supports fuzzy match)', 'zh-CN': '要合并的分支名（支持模糊匹配，不传则列出所有分支供选择）' },
  MERGE_OPTION_MESSAGE: { en: 'Commit message (required if target worktree has modifications)', 'zh-CN': '提交信息（目标 worktree 工作区有修改时必填）' },
  MERGE_OPTION_AUTO: { en: 'Auto-resolve conflicts with AI without asking', 'zh-CN': '遇到冲突直接调用 AI 解决，不再询问' },
  // remove 命令
  REMOVE_DESC: { en: 'Remove worktree (supports fuzzy match/multi-select/all)', 'zh-CN': '移除 worktree（支持模糊匹配/多选/全部）' },
  REMOVE_OPTION_ALL: { en: 'Remove all worktrees for the current project', 'zh-CN': '移除当前项目下所有 worktree' },
  REMOVE_OPTION_BRANCH: { en: 'Branch name (supports fuzzy match)', 'zh-CN': '指定分支名（支持模糊匹配，不传则列出所有分支）' },
  // resume 命令
  RESUME_DESC: { en: 'Resume a Claude Code session in an existing worktree', 'zh-CN': '在已有 worktree 中恢复 Claude Code 会话（支持多选批量恢复）' },
  RESUME_OPTION_BRANCH: { en: 'Branch to resume (supports fuzzy match)', 'zh-CN': '要恢复的分支名（支持模糊匹配，不传则列出所有分支）' },
  RESUME_OPTION_PROMPT: { en: 'Non-interactive follow-up (requires -b)', 'zh-CN': '非交互式追问（需配合 -b 指定分支）' },
  RESUME_OPTION_FILE: { en: 'Batch follow-up from task file (matches by branch name)', 'zh-CN': '从任务文件批量追问（通过 branch 名匹配已有 worktree）' },
  RESUME_OPTION_CONCURRENCY: { en: 'Max concurrency for batch follow-up, 0 means unlimited', 'zh-CN': '批量追问最大并发数，0 表示不限制' },
  // sync 命令
  SYNC_DESC: { en: 'Sync main branch code to target worktree', 'zh-CN': '将主分支最新代码同步到目标 worktree' },
  SYNC_OPTION_BRANCH: { en: 'Branch to sync (supports fuzzy match)', 'zh-CN': '要同步的分支名（支持模糊匹配，不传则列出所有分支）' },
  // status 命令
  STATUS_DESC: { en: 'Show project status overview (supports --json)', 'zh-CN': '显示项目全局状态总览（支持 --json 格式输出）' },
  STATUS_OPTION_JSON: { en: 'Output in JSON format', 'zh-CN': '以 JSON 格式输出' },
  STATUS_OPTION_INTERACTIVE: { en: 'Interactive panel mode', 'zh-CN': '交互式面板模式' },
  // list 命令
  LIST_DESC: { en: 'List all worktrees (supports --json)', 'zh-CN': '列出当前项目所有 worktree（支持 --json 格式输出）' },
  LIST_OPTION_JSON: { en: 'Output in JSON format', 'zh-CN': '以 JSON 格式输出' },
  // reset 命令
  RESET_DESC: { en: 'Reset main worktree working directory and staging area', 'zh-CN': '重置主 worktree 工作区和暂存区（保留 validate 快照）' },
  // home 命令
  HOME_DESC: { en: 'Switch back to the main work branch', 'zh-CN': '切换回主工作分支' },
  // projects 命令
  PROJECTS_DESC: { en: 'Show worktree overview across projects', 'zh-CN': '展示所有项目的 worktree 概览，或查看指定项目的 worktree 详情' },
  PROJECTS_OPTION_JSON: { en: 'Output in JSON format', 'zh-CN': '以 JSON 格式输出' },
  // alias 命令
  ALIAS_DESC: { en: 'Manage command aliases (list / set / remove)', 'zh-CN': '管理命令别名（列出 / 设置 / 移除）' },
  ALIAS_LIST_DESC: { en: 'List all aliases', 'zh-CN': '列出所有别名' },
  ALIAS_SET_DESC: { en: 'Set a command alias', 'zh-CN': '设置命令别名' },
  ALIAS_REMOVE_DESC: { en: 'Remove a command alias', 'zh-CN': '移除命令别名' },
  // completion 命令
  COMPLETION_DESC: { en: 'Internal dynamic completion method (not public)', 'zh-CN': '内部使用的动态补全方法，不对外公开' },
  // tasks 命令
  TASKS_DESC: { en: 'Task file management', 'zh-CN': '任务文件管理' },
  TASKS_INIT_DESC: { en: 'Generate task template file', 'zh-CN': '生成任务模板文件' },
  // cover 命令
  COVER_DESC: { en: 'Cover validate branch changes back to target worktree', 'zh-CN': '将验证分支上的修改覆盖回目标 worktree（自动推导目标分支）' },
};

export const CLI_DESCRIPTIONS = createMessages(CLI_DESCRIPTIONS_I18N);
```

- [ ] **Step 2: 在 messages/index.ts 中导入导出**

```typescript
export { CLI_DESCRIPTIONS } from './cli-descriptions.js';
```

- [ ] **Step 3: Commit**

```bash
git add src/constants/messages/cli-descriptions.ts src/constants/messages/index.ts
git commit -m "feat(i18n): add centralized CLI descriptions with en/zh-CN mapping"
```

---

### Task 6: 非 messages 常量文件 i18n 化

**Files:**
- Modify: `src/constants/progress.ts`
- Modify: `src/constants/prompt.ts`
- Modify: `src/constants/ai-prompts.ts`
- Modify: `src/constants/tasks-template.ts`
- Modify: `src/constants/config.ts`
- Modify: `src/constants/project-config.ts`

- [ ] **Step 1: 双语化 progress.ts**

将 `TASK_STATUS_LABELS` 和 `TEXT_ACTIVITY_PREFIX` 改为函数调用形式：

```typescript
import { getCurrentLanguage } from '../utils/i18n.js';

/** 任务状态标签（根据语言返回） */
export function getTaskStatusLabels() {
  const lang = getCurrentLanguage();
  return lang === 'en'
    ? { PENDING: 'Pending', RUNNING: 'Running', DONE: 'Done', FAILED: 'Failed' }
    : { PENDING: '排队中', RUNNING: '运行中', DONE: '完成', FAILED: '失败' };
}

/** 文本类型活动的前缀 */
export function getTextActivityPrefix() {
  return getCurrentLanguage() === 'en' ? 'Thinking' : '思考中';
}
```

更新所有引用 `TASK_STATUS_LABELS` 和 `TEXT_ACTIVITY_PREFIX` 的代码。

- [ ] **Step 2: 双语化 prompt.ts**

将 `UNKNOWN_DATE_GROUP`、`GROUP_SEPARATOR_LABEL`、`UNKNOWN_DATE_SEPARATOR_LABEL` 改为函数调用形式。

- [ ] **Step 3: 改写 ai-prompts.ts 为英文**

将 `CONFLICT_RESOLVE_PROMPT` 的中文内容改为英文：

```typescript
export const CONFLICT_RESOLVE_PROMPT = `You are a Git merge conflict resolution expert. The repository is currently in a merge conflict state.

## Task

1. Use git status and git diff to examine the list of conflicted files and their contents
2. Use git log to analyze the change intent of each branch
3. Directly edit each conflicted file, removing all conflict markers (<<<<<<<, =======, >>>>>>>)
4. Preserve meaningful changes from both sides, merging code logic appropriately
5. If both branches modified the same section with different intents, prioritize code correctness and completeness
6. After resolving conflicts, ensure code syntax is correct and logic is complete
7. Do not add any comments explaining your modifications, only modify file contents

Please begin.`;
```

- [ ] **Step 4: 双语化 tasks-template.ts**

将 `TASK_TEMPLATE_CONTENT` 改为函数形式，根据语言返回对应模板内容。

- [ ] **Step 5: 双语化 config.ts 和 project-config.ts 的 description 字段**

将 `CONFIG_DEFINITIONS` 和 `PROJECT_CONFIG_DEFINITIONS` 中的 `description` 改为根据语言返回的函数形式。由于 description 是在交互式配置编辑器中使用的，改为 `getDescription()` 函数。

- [ ] **Step 6: 运行测试验证**

Run: `npx vitest run`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add src/constants/progress.ts src/constants/prompt.ts src/constants/ai-prompts.ts src/constants/tasks-template.ts src/constants/config.ts src/constants/project-config.ts
git commit -m "feat(i18n): bilingualize non-messages constants (progress, prompt, ai-prompts, tasks-template, config descriptions)"
```

---

### Task 7: 散落硬编码中文替换 — utils 层

**Files:**
- Modify: `src/utils/config-strategy.ts`
- Modify: `src/utils/formatter.ts`
- Modify: `src/utils/dry-run.ts`
- Modify: `src/utils/prompt.ts`
- Modify: `src/utils/ui-prompts.ts`
- Modify: `src/utils/validate-branch.ts`
- Modify: `src/utils/terminal.ts`
- Modify: `src/utils/claude.ts`
- Modify: `src/utils/task-executor.ts`
- Modify: `src/utils/worktree-matcher.ts`
- Modify: `src/utils/alias.ts`
- Modify: `src/utils/interactive-panel-render.ts`
- Modify: `src/utils/validate-runner.ts`

- [ ] **Step 1: 逐文件替换硬编码中文为消息常量引用**

每个文件中：
1. 找到所有硬编码中文字符串
2. 替换为从 `MESSAGES` 导入的对应常量
3. 确保导入路径正确

**具体替换映射：**

`config-strategy.ts`:
- `(未设置)` → `MESSAGES.NOT_SET`
- `非交互模式下无法使用交互式配置编辑器，请使用 clawt config set <key> <value>` → `MESSAGES.NON_INTERACTIVE_CONFIG_EDITOR`
- `请输入有效的数字` → `MESSAGES.INVALID_NUMBER_PROMPT`

`formatter.ts`:
- `是否继续？` → `MESSAGES.CONFIRM_CONTINUE`
- `无变更` → `MESSAGES.NO_CHANGES`
- `(未提交修改)` → `MESSAGES.UNCOMMITTED_CHANGES`
- `${status.commitCount} 个提交` → `MESSAGES.COMMIT_COUNT(status.commitCount)`
- `刚刚` → `MESSAGES.JUST_NOW`
- `${diffMinutes} 分钟前` → `MESSAGES.MINUTES_AGO(diffMinutes)`
- `${diffHours} 小时前` → `MESSAGES.HOURS_AGO(diffHours)`
- `${diffDays} 天前` → `MESSAGES.DAYS_AGO(diffDays)`
- `${months} 个月前` → `MESSAGES.MONTHS_AGO(months)`
- `${years} 年前` → `MESSAGES.YEARS_AGO(years)`

`ui-prompts.ts`:
- `非交互模式下无法进行分支选择...` → `MESSAGES.NON_INTERACTIVE_BRANCH_SELECT`
- `非交互模式下无法进行分支多选...` → `MESSAGES.NON_INTERACTIVE_MULTI_SELECT`

`worktree-matcher.ts`:
- `今天` → `MESSAGES.TODAY`
- `昨天` → `MESSAGES.YESTERDAY`

其他文件同理按 Task 4 的迁移映射替换。

- [ ] **Step 2: 运行测试验证**

Run: `npx vitest run`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add src/utils/config-strategy.ts src/utils/formatter.ts src/utils/dry-run.ts src/utils/prompt.ts src/utils/ui-prompts.ts src/utils/validate-branch.ts src/utils/terminal.ts src/utils/claude.ts src/utils/task-executor.ts src/utils/worktree-matcher.ts src/utils/alias.ts src/utils/interactive-panel-render.ts src/utils/validate-runner.ts
git commit -m "feat(i18n): replace hardcoded Chinese in utils with message constants"
```

---

### Task 8: 散落硬编码中文替换 — commands + hooks + index

**Files:**
- Modify: `src/commands/config.ts`
- Modify: `src/commands/status.ts`
- Modify: `src/commands/remove.ts`
- Modify: `src/commands/create.ts`
- Modify: `src/commands/resume.ts`
- Modify: `src/commands/merge.ts`
- Modify: `src/commands/sync.ts`
- Modify: `src/commands/cover-validate.ts`
- Modify: `src/commands/validate.ts`
- Modify: `src/commands/reset.ts`
- Modify: `src/commands/list.ts`
- Modify: `src/commands/init.ts`
- Modify: `src/commands/run.ts`
- Modify: `src/commands/projects.ts`
- Modify: `src/commands/home.ts`
- Modify: `src/commands/alias.ts`
- Modify: `src/commands/completion.ts`
- Modify: `src/commands/tasks.ts`
- Modify: `src/hooks/post-create.ts`
- Modify: `src/index.ts`

- [ ] **Step 1: 替换所有 Commander .description() 和 .option() 为 CLI_DESCRIPTIONS 引用**

每个命令注册函数中，将硬编码中文描述替换为 `CLI_DESCRIPTIONS.XXX`。例如：

```typescript
// 改造前
.description('交互式查看和修改全局配置')
// 改造后
.description(CLI_DESCRIPTIONS.CONFIG_DESC)
```

- [ ] **Step 2: 替换命令实现中的硬编码中文为消息常量引用**

各命令文件中的硬编码中文替换为 `MESSAGES.XXX`。例如：

`commands/config.ts`:
- `当前配置将被覆盖为默认值` → `MESSAGES.CONFIG_RESET_WARNING`

`commands/status.ts`:
- `✓ 干净` → `MESSAGES.STATUS_CLEAN`
- `✗ 有未提交修改` → `MESSAGES.STATUS_DIRTY`
- `个本地提交` → 通过消息常量函数处理
- 其他散落中文同理

其他命令文件同理。

- [ ] **Step 3: 替换 src/index.ts 中的硬编码中文**

- `.description(...)` → `CLI_DESCRIPTIONS.PROGRAM_DESC`
- `未知错误` → `MESSAGES.UNKNOWN_ERROR`

- [ ] **Step 4: 替换 hooks/post-create.ts 中的硬编码中文**

- `项目配置 (postCreate)` → 对应消息常量

- [ ] **Step 5: 运行测试验证**

Run: `npx vitest run`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/commands/ src/hooks/ src/index.ts
git commit -m "feat(i18n): replace hardcoded Chinese in commands/hooks/index with message constants and CLI descriptions"
```

---

### Task 9: README 更新

**Files:**
- Modify: `README.md`
- Modify: `README.zh-CN.md`

- [ ] **Step 1: 在 README.md 顶部添加 GitHub 仓库地址**

在标题下方添加：
```markdown
> **Repository:** [https://github.com/afk101/clawt](https://github.com/afk101/clawt)
```

- [ ] **Step 2: 在 README.zh-CN.md 顶部添加 GitHub 仓库地址**

在标题下方添加：
```markdown
> **仓库地址：**[https://github.com/afk101/clawt](https://github.com/afk101/clawt)
```

- [ ] **Step 3: Commit**

```bash
git add README.md README.zh-CN.md
git commit -m "docs: add GitHub repository URL to README files"
```

---

### Task 10: 语言缓存刷新 + config 命令集成

**Files:**
- Modify: `src/commands/config.ts`

- [ ] **Step 1: 在 config set 成功后刷新语言缓存**

当用户通过 `clawt config set language xxx` 修改语言后，调用 `resetLanguageCache()` 使下次消息读取时使用新语言：

```typescript
import { resetLanguageCache } from '../utils/i18n.js';

// 在 handleConfigSet 中，设置成功后：
if (key === 'language') {
  resetLanguageCache();
}
```

- [ ] **Step 2: 在 config reset 成功后刷新语言缓存**

`handleConfigReset()` 中恢复默认配置后调用 `resetLanguageCache()`。

- [ ] **Step 3: 运行测试验证**

Run: `npx vitest run`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add src/commands/config.ts
git commit -m "feat(i18n): reset language cache after config set/reset"
```

---

### Task 11: 端到端验证

- [ ] **Step 1: 构建项目**

Run: `npm run build`
Expected: 构建成功，无 TypeScript 错误

- [ ] **Step 2: 验证英文默认输出**

Run: `clawt --help`
Expected: 所有描述和帮助文本为英文

- [ ] **Step 3: 切换到中文**

Run: `clawt config set language zh-CN && clawt --help`
Expected: 所有描述和帮助文本为中文

- [ ] **Step 4: 切换回英文**

Run: `clawt config set language en && clawt --help`
Expected: 所有描述和帮助文本为英文

- [ ] **Step 5: 验证 config get language**

Run: `clawt config get language`
Expected: 输出 `en`

- [ ] **Step 6: 运行全量测试**

Run: `npx vitest run`
Expected: 所有测试 PASS

- [ ] **Step 7: 最终 Commit**

```bash
git add -A
git commit -m "feat(i18n): complete i18n integration with en/zh-CN support, default to English"
```
